1. 2, 같은 이름의 함수를 여러개 선언하는 것은 함수 중복 (함수 오버로딩) 이라고 한다.

2. 3, 클래스의 멤버함수도 중복할 수 있다. (당장 생성자도 중복이 되는데 뭐..)
   C언어네는 함수 중복이 없고, 매개변수 타입이나 개수가 다른 같은 이름의 함수는 중복이 가능하며, 리턴타입만 다르면 중복이 되지 않는다.
   (모호성은 물론 고민해야한다)

3. 3,4 는 같이 선언할 수 없다.

4. 1,2 는 같이 선언할 수 없다.

5. 2, 함수 중복은 컴파일러가 처리한다.

6. 2, 함수 중복과 호출문의 연결(바인딩)은 컴파일 시 이루어진다.

7. 4, 매개변수는 2개밖에 없는데 3개를 넣을 순 없다. 3번 처럼 return 타입이 있더라도 함수 단독 호출이 가능하다.

8. 2, 역시 매개변수는 2개인데 3개를 넣을 순 없다.

9. 3, 디폴트 매개변수는 오른쪽에서부터 써야한다.

10. 2, 두번째 매개변수로 들어온 것이 값인지 참조인지 모호하다.

11. 4, 소멸자는 매개변수를 가질 수 없다.

12. 
(1) 0
(2) 3
(3) 5hello

13. f(10)을 호출하면 f(10, 0) 을 호출해야 하는지, f(10) 을 호출해야 하는지 모호하다.

14. f(10)을 호출하면, 매개변수로 들어온 int 타입의 10을 float으로 변환할지, double로 변환할지 모호하여 컴파일 오류가 발생한다.

15. 4, static멤버 함수는 non-static멤버 함수/변수에 접근할 수 없다.

16. 3, 객체에는 :: 연산자를 사용할 수 없다. 클래스 명에 :: 을 사용해야 한다.
